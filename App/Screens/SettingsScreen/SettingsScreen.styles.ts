import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  LanguageButton: {
    width: 150,
    height: 70,
    margin: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
